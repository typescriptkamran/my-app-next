'use client';
import React from 'react'

const AddtoCart = () => {
  return (
    <div>
        <button className ="btn btn-active btn-primary" onClick={() => console.log('Click')}> Add to Cart </button>
    </div>
  )
}

export default AddtoCart